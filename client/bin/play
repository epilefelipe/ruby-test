#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require_relative '../lib/mansion_velasco'

api_url = ARGV[0] || ENV.fetch('API_URL', 'http://localhost:3000')

game = MansionVelasco::Game.new(api_url: api_url)
game.start
